prefix: ./env
channels:
  - conda-forge
  - anaconda
  - defaults
dependencies:
  - python=3.8
  - xarray==0.15
  - cartopy
  - matplotlib
  - pip
  - pip:
    - -r dev-requirements.txt
    - -r requirements.txt
