prefix: ./env
channels:
  - conda-forge
  - anaconda
  - defaults
dependencies:
  - python=3.8
  - xarray==0.15
  - cartopy
  - matplotlib==3.2.2
  - pip
  - pip:
    - -r file:dev-requirements.txt
    - -r file:requirements.txt
